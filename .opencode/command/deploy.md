---
description: git add + commit + push 到 GitHub Pages
---

請執行以下流程：

1. 執行 `git status` 並顯示結果
2. 執行 `git add .`
3. 用 question 工具詢問用戶 commit 內容
4. 執行 `git commit -m "{用戶提供的訊息}"`
5. 執行 `git push origin main`
